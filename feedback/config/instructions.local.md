# Per-meeting overlay (optional)

This file is concatenated after the global instructions. Use it for the focus of
a single meeting; leave it empty otherwise.

Example (delete or replace):

- Focus: the auth-service -> gateway cutover. Watch for anyone conflating who
  owns token refresh (auth-service owns it, not the gateway).
